services:
  grobid:
    image: lfoppiano/grobid:0.8.2
    ports:
      - "8070:8070"

  aiandos:
    build: .
    depends_on:
      - grobid
    volumes:
      - ./dataset:/app/dataset
      - ./output:/app/output